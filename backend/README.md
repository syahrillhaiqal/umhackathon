# GridGuard Backend

FastAPI + LangGraph backend for autonomous municipal hazard resolution.

## Run

```bash
uv sync
uv run uvicorn app.main:app --reload
```

## Environment

Copy `.env.example` to `.env` and update credentials.
